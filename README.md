# Tahfizh Dashboard

Node.js + Express + MySQL MVC application for Halaqoh clustering.

Environment: copy `.env.example` to `.env` and set DB credentials (database name must be `tahfizh`).

Run:

```bash
npm install
npm run dev
```
